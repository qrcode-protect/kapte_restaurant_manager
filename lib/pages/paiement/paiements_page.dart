import 'package:flutter/material.dart';

class PaiementsPage extends StatelessWidget {
  const PaiementsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PaiementsPage'),
      ),
    );
  }
}
